# 奇点同行学习站 V2

这是一个无需安装依赖的静态网页。

## 使用方式

1. 解压 ZIP 文件。
2. 双击 `index.html` 即可在浏览器中预览。
3. 部署到服务器、对象存储或静态网站服务时，上传整个文件夹，并将 `index.html` 设为首页。

## 当前状态

- 三个课程的腾讯会议链接已写入页面。
- “提交作业、请假申请、学习资料库、课程回放”暂为示意入口，需要在 `index.html` 中将对应的 `href="#"` 替换为真实链接。
- 页面不依赖外部字体、图片或第三方代码。
